<%= form_for(@bahan) do |f| %>
  <% if @bahan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bahan.errors.count, "error") %> prohibited this bahan from being saved:</h2>

      <ul>
      <% @bahan.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nama %><br />
    <%= f.text_field :nama %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
